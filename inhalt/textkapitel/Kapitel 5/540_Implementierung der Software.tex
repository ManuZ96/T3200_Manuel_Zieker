\section{Implementierung der Software}
% Die Softwareentwicklung erfolgt schrittweise und modular.
% Zunächst werden grundlegende Funktionen implementiert.
% Dazu zählen die Initialisierung der Hardware sowie die Ansteuerung der LED-Streifen.
% Die LED-Streifen werden über die Bibliothek \texttt{Adafruit\_NeoPixel} angesteuert.
% Dabei werden für den Hauptstreifen, die Sekundenanzeige und den Zusatzstreifen eigene Objekte angelegt.

% \begin{lstlisting}[style=arduino,caption={Initialisierung der LED-Streifen}]
% #define LED_PIN            1
% #define LED_COUNT          256

% #define LED_PIN_SEC        7
% #define LED_COUNT_SEC      60

% #define LED_PIN_THIRD      10
% #define LED_COUNT_THIRD    38

% Adafruit_NeoPixel strip(LED_COUNT, LED_PIN, NEO_GRB + NEO_KHZ800);
% Adafruit_NeoPixel secondStrip(LED_COUNT_SEC, LED_PIN_SEC, NEO_GRB + NEO_KHZ800);
% Adafruit_NeoPixel thirdStrip(LED_COUNT_THIRD, LED_PIN_THIRD, NEO_GRB + NEO_KHZ800);
% \end{lstlisting}

% Eine zentrale Hilfsfunktion stellt die gezielte Aktivierung einzelner LEDs dar.
% Vor dem Setzen eines Farbwertes wird geprüft, ob der angegebene LED-Index innerhalb des gültigen Bereichs liegt.

% \begin{lstlisting}[style=arduino,caption={Ansteuerung einer einzelnen LED}]
% void setLed(int ledNumber, uint8_t r, uint8_t g, uint8_t b) {
%   if (ledNumber >= 0 && ledNumber < LED_COUNT) {
%     strip.setPixelColor(ledNumber, strip.Color(r, g, b));
%   }
% }
% \end{lstlisting}

% Für die gleichzeitige Ansteuerung mehrerer LEDs werden Arrays verwendet.
% Diese enthalten die Indizes der LEDs, die ein bestimmtes Wort oder Symbol darstellen.
% Die folgende Funktion durchläuft ein solches Array und setzt alle enthaltenen LEDs auf denselben RGB-Farbwert.

% \begin{lstlisting}[style=arduino,caption={Ansteuerung mehrerer LEDs ueber ein Array}]
% void setLeds(const int leds[], int count, uint8_t r, uint8_t g, uint8_t b) {
%   for (int i = 0; i < count; i++) {
%     setLed(leds[i], r, g, b);
%   }
% }
% \end{lstlisting}

% Die Helligkeit wird softwareseitig über einen Prozentwert skaliert.
% Dadurch können die RGB-Werte über die Weboberfläche angepasst werden, ohne die Grundlogik der Anzeige zu verändern.

% \begin{lstlisting}[style=arduino,caption={Skalierung der Farbwerte anhand der eingestellten Helligkeit}]
% uint8_t applyBrightness(uint8_t colorValue) {
%   return (uint8_t)((colorValue * webBrightness) / 100);
% }
% \end{lstlisting}

% Beim Systemstart wird ein Boot-Modus verwendet.
% Dabei werden die LEDs des Hauptstreifens nacheinander eingeschaltet, kurz eingeschaltet gehalten und anschließend rückwärts wieder ausgeschaltet.
% Die Animation läuft solange, bis die für den Betrieb erforderlichen Initialisierungsschritte abgeschlossen sind.

% \begin{lstlisting}[style=arduino,caption={Boot-Modus des Hauptstreifens}]
% void bootModeUpdate() {
%   static int state = 0;
%   static int index = 0;
%   static unsigned long lastStep = 0;
%   static unsigned long waitStart = 0;

%   const unsigned long STEP_INTERVAL_MS = 8;
%   const unsigned long WAIT_INTERVAL_MS = 1000;

%   unsigned long now = millis();

%   switch (state) {
%     case 0:
%       if (now - lastStep >= STEP_INTERVAL_MS) {
%         lastStep = now;
%         strip.setPixelColor(index, strip.Color(ledR, ledG, ledB));
%         strip.show();
%         index++;

%         if (index >= LED_COUNT) {
%           index = LED_COUNT - 1;
%           waitStart = now;
%           state = 1;
%         }
%       }
%       break;

%     case 1:
%       if (now - waitStart >= WAIT_INTERVAL_MS) {
%         state = 2;
%       }
%       break;

%     case 2:
%       if (now - lastStep >= STEP_INTERVAL_MS) {
%         lastStep = now;
%         strip.setPixelColor(index, strip.Color(0, 0, 0));
%         strip.show();
%         index--;

%         if (index < 0) {
%           index = 0;
%           waitStart = now;
%           state = 3;
%         }
%       }
%       break;

%     case 3:
%       if (now - waitStart >= WAIT_INTERVAL_MS) {
%         state = 0;
%       }
%       break;
%   }
% }
% \end{lstlisting}

% Die Verarbeitung der Zeit erfolgt durch zyklisches Auslesen der lokalen Systemzeit.
% Beim Start wird die Uhrzeit über NTP synchronisiert.
% Im Betrieb werden Stunde, Minute und Sekunde in globalen Variablen gespeichert.

% \begin{lstlisting}[style=arduino,caption={Aktualisierung der lokalen Zeitvariablen}]
% bool updateTimeVariables() {
%   struct tm timeinfo;

%   if (!getLocalTime(&timeinfo, 50)) {
%     return false;
%   }

%   currentHour   = timeinfo.tm_hour;
%   currentMinute = timeinfo.tm_min;
%   currentSecond = timeinfo.tm_sec;

%   return true;
% }
% \end{lstlisting}

% Die zentrale Anzeigefunktion der Wortuhr löscht zunächst den Hauptstreifen.
% Anschließend werden feste Anzeigeelemente, Minutenwörter, das passende Stundenwort sowie Wetterinformationen gesetzt.
% Am Ende wird die Anzeige mit \texttt{strip.show()} aktualisiert.

% \begin{lstlisting}[style=arduino,caption={Zentrale Anzeigefunktion der Wortuhr}]
% void showHourWord(int hour24) {
%   strip.clear();

%   if (!lampPower) {
%     strip.show();
%     return;
%   }

%   showAlwaysOnLeds();

%   int hourToShow = getDisplayHour(hour24, currentMinute);

%   showMinuteWords(currentMinute);
%   showHourNumber(hourToShow, currentMinute);

%   showWeatherStatusLeds();

%   strip.show();
% }
% \end{lstlisting}

% Die Umrechnung der Stunde berücksichtigt die sprachliche Darstellung der Uhrzeit.
% Ab Minute 20 bezieht sich die Anzeige auf die folgende Stunde, beispielsweise bei der Darstellung von \glqq halb zwei\grqq.

% \begin{lstlisting}[style=arduino,caption={Bestimmung der anzuzeigenden Stunde}]
% int getDisplayHour(int hour24, int minute) {
%   if (hour24 == 0) {
%     if (minute < 20) {
%       return 0;
%     } else {
%       return 1;
%     }
%   }

%   int displayHour = hour24;

%   if (minute >= 20) {
%     displayHour++;
%   }

%   int hour12 = displayHour % 12;

%   if (hour12 == 0) {
%     hour12 = 12;
%   }

%   return hour12;
% }
% \end{lstlisting}

% Neben der Uhrzeit werden aktuelle Wetterdaten über eine HTTPS-Abfrage geladen.
% Aus dem empfangenen JSON-Datensatz werden Außentemperatur, Wettercode und Tag-/Nachtstatus ausgelesen.
% Der Wettercode wird anschließend einer Wettergruppe zugeordnet.

% \begin{lstlisting}[style=arduino,caption={Klassifikation des Wettercodes}]
% WeatherGroup classifyWeatherCode(int code) {
%   if (code == 0) {
%     return WEATHER_CLEAR;
%   }

%   if (code >= 1 && code <= 3) {
%     return WEATHER_CLOUDY;
%   }

%   if (code == 45 || code == 48) {
%     return WEATHER_FOG;
%   }

%   if ((code >= 51 && code <= 67) || (code >= 80 && code <= 82)) {
%     return WEATHER_RAIN;
%   }

%   if (code >= 71 && code <= 77) {
%     return WEATHER_SNOW;
%   }

%   if (code == 96 || code == 99) {
%     return WEATHER_HAIL;
%   }

%   if (code == 95) {
%     return WEATHER_STORM;
%   }

%   return WEATHER_UNKNOWN;
% }
% \end{lstlisting}

% Für Innenraumtemperatur und Luftfeuchtigkeit wird ein DHT22-Sensor eingesetzt.
% Die Messwerte werden zyklisch aktualisiert und für die Zusatzanzeige verwendet.

% \begin{lstlisting}[style=arduino,caption={Auslesen des DHT22-Sensors}]
% void updateIndoorSensor() {
%   float h = dht.readHumidity();
%   float t = dht.readTemperature();

%   if (isnan(h) || isnan(t)) {
%     Serial.println("DHT-Sensor konnte nicht gelesen werden.");
%     indoorSensorValid = false;
%     return;
%   }

%   indoorHumidity = h;
%   indoorTemperature = t;
%   indoorSensorValid = true;
%   lastIndoorSensorUpdate = millis();
% }
% \end{lstlisting}

% Der dritte LED-Streifen fasst mehrere Zusatzinformationen zusammen.
% Dazu gehören Raumfeuchte, Mondphase, Innenraumtemperatur und Tageszeit.

% \begin{lstlisting}[style=arduino,caption={Aktualisierung des Zusatzstreifens}]
% void showThirdStripStatus() {
%   thirdStrip.clear();

%   if (!lampPower) {
%     thirdStrip.show();
%     return;
%   }

%   showHumidityStatus();
%   showMoonPhaseStatus();
%   showIndoorTemperatureStatus();
%   showDaytimeStatus();

%   thirdStrip.show();
% }
% \end{lstlisting}

% Zur Bedienung und Überwachung des Systems wird ein HTTP-Server auf dem ESP32 gestartet.
% Die Weboberfläche zeigt aktuelle Statuswerte an und erlaubt die Einstellung von Ein-/Aus-Zustand, Helligkeit und Farbe.

% \begin{lstlisting}[style=arduino,caption={Registrierung der Webserver-Routen}]
% void setupHttpServer() {
%   server.on("/", handleRoot);
%   server.on("/api/status", handleApiStatus);
%   server.on("/set", handleSet);
%   server.on("/weather", handleWeatherNow);
%   server.onNotFound(handleNotFound);

%   server.begin();
% }
% \end{lstlisting}

% Die Hauptfunktionen des Programms sind \texttt{setup()} und \texttt{loop()}.
% In \texttt{setup()} werden Hardware, Sensorik, WLAN, Zeitsynchronisation, Wetterdaten und Webserver initialisiert.
% In \texttt{loop()} werden Webserver-Anfragen verarbeitet und alle Anzeigen zyklisch aktualisiert.

% \begin{lstlisting}[style=arduino,caption={Zyklische Programmlogik}]
% void loop() {
%   server.handleClient();

%   static unsigned long lastClockUpdate = 0;
%   static unsigned long lastTimePrint = 0;

%   unsigned long nowMs = millis();

%   if (nowMs - lastClockUpdate >= 500) {
%     lastClockUpdate = nowMs;

%     if (updateTimeVariables()) {
%       checkDailyResync();
%       checkPeriodicWeatherUpdate();
%       checkPeriodicIndoorSensorUpdate();

%       showHourWord(currentHour);
%       showSecondLed(currentSecond);
%       showThirdStripStatus();
%     }
%   }

%   if (nowMs - lastTimePrint >= 2000) {
%     lastTimePrint = nowMs;

%     Serial.printf("Aktuelle Zeit: %02d:%02d:%02d | RGB=(%d,%d,%d) | On=%d | Brightness=%d | Temp=%.1f | Code=%d | WetterValid=%d\n",
%                   currentHour, currentMinute, currentSecond,
%                   ledR, ledG, ledB, lampPower, webBrightness,
%                   outsideTemperature, weatherCode, weatherValid);
%   }
% }
% \end{lstlisting}

% Die Software wird iterativ aufgespielt und erweitert.
% Nach jeder Erweiterung erfolgt eine Funktionsprüfung.
% Dieses Vorgehen ermöglicht eine schrittweise Integration aller Systemfunktionen und vereinfacht die Fehlersuche bei Problemen.




Die Softwareentwicklung erfolgt schrittweise und modular.  
Zunächst werden grundlegende Funktionen zur Initialisierung der Hardware sowie zur Ansteuerung der LED-Streifen implementiert.  
Für die Ansteuerung der LEDs wird die Bibliothek \texttt{Adafruit\_NeoPixel} verwendet.  
Dabei werden für den Hauptstreifen, die Sekundenanzeige und den Zusatzstreifen separate Objekte angelegt.

\begin{lstlisting}[style=arduino,caption={Initialisierung der LED-Streifen}]
#define LED_PIN            1
#define LED_COUNT          256

#define LED_PIN_SEC        7
#define LED_COUNT_SEC      60

#define LED_PIN_THIRD      10
#define LED_COUNT_THIRD    38

Adafruit_NeoPixel strip(LED_COUNT, LED_PIN, NEO_GRB + NEO_KHZ800);
Adafruit_NeoPixel secondStrip(LED_COUNT_SEC, LED_PIN_SEC, NEO_GRB + NEO_KHZ800);
Adafruit_NeoPixel thirdStrip(LED_COUNT_THIRD, LED_PIN_THIRD, NEO_GRB + NEO_KHZ800);
\end{lstlisting}

Eine zentrale Hilfsfunktion stellt die gezielte Aktivierung einzelner LEDs dar.  
Vor dem Setzen eines Farbwertes wird geprüft, ob sich der LED-Index innerhalb des gültigen Bereichs befindet.

\begin{lstlisting}[style=arduino,caption={Ansteuerung einer einzelnen LED}]
void setLed(int ledNumber, uint8_t r, uint8_t g, uint8_t b) {
  if (ledNumber >= 0 && ledNumber < LED_COUNT) {
    strip.setPixelColor(ledNumber, strip.Color(r, g, b));
  }
}
\end{lstlisting}

Für die Darstellung der Uhrzeit werden die LEDs bestimmten Wörtern zugeordnet.  
Die Verarbeitung der Zeit erfolgt durch zyklisches Auslesen der lokalen Systemzeit.  
Die zentrale Anzeigefunktion löscht zunächst den Hauptstreifen und aktiviert anschließend die benötigten Wörter für Stunden-, Minuten- und Zusatzinformationen.

\begin{lstlisting}[style=arduino,caption={Zentrale Anzeigefunktion der Wortuhr}]
void showHourWord(int hour24) {
  strip.clear();

  if (!lampPower) {
    strip.show();
    return;
  }

  showAlwaysOnLeds();

  int hourToShow = getDisplayHour(hour24, currentMinute);

  showMinuteWords(currentMinute);
  showHourNumber(hourToShow, currentMinute);

  showWeatherStatusLeds();

  strip.show();
}
\end{lstlisting}

Zusätzlich werden Wetterinformationen, Innenraumtemperatur, Luftfeuchtigkeit, Mondphase und Tageszeit über weitere LED-Bereiche dargestellt.  
Die Helligkeitssteuerung erfolgt softwareseitig über eine Skalierung der RGB-Werte.  
Darüber hinaus werden Funktionen zur Zeitsynchronisation, Wetterklassifikation, Sensorabfrage und Zusatzanzeigen implementiert.
Die Software wird iterativ erweitert und nach jeder Erweiterung getestet.  
Dieses Vorgehen ermöglicht eine schrittweise Integration aller Systemfunktionen und vereinfacht die Fehlersuche.
Der vollständige Programmcode befindet sich in Anhang~\ref{apx:1}.
